import 'package:advanced_mobile/generated/l10n.dart';

Map<String,String> languageSettingMapping(context) => {
  "english": S.of(context).english,
  "vietnamese": S.of(context).vietnamese
};

Map<String,String> themeMapping(context) => {
  "dark": S.of(context).dark,
  "light": S.of(context).light
};